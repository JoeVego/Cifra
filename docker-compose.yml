#docker-desktop personal
#docker compose up -d --build
#docker exec -it my_container bash - переход в команднуб строку для
#внтури редиса есть 16 команд Redis CLI


#иниц редиса в питон проекте
#pip install redis
#redis_server = redis.Redis(host, port, и индекс БД)


#в йоло класс перменнуб реди_сервер
# с инцииалищацией с блока выше

# в йоло классе делаем сет методов
# а в функции генераторе получаем кадр с поомщью гет

#после succes делаем сет
# камера айди оставили постоянным не обновляем его, перезаписываем
# кадр храним в редисе в виде байтов - base64
# через base64.encode кодируем изображение

# в докере начинаем с #redis-cli
# keys * - ключи - айди камеры выведет

#в браузере объекту имейдж указывает эндпоин и получаем видео
# video_feed методе изменили чтобы динамический передавался камера айди

#
#
#
#



version: '3'
services: